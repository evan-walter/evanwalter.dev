generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
model GuestbookPost {
  id        String     @id @default(cuid())
  name      String
  message   String
}
